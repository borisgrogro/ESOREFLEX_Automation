# ESOREFLEX_Automation
Shell script watches a specified directory for added files, then executes a python script once it detects an event. The python script launches esoreflex and processes the new file(s).
